[package]
name = "octoevents-worker-example"
version = "0.0.0"
edition = "2024"
rust-version = "1.88"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
octoevents = { path = "../..", default-features = false, features = ["http"] }
serde_json = "1.0.143"
worker = { version = "0.8.5", features = ["http"] }
